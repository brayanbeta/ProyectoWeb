import { FiltroDetalleProductosPipe } from './filtro-detalle-productos.pipe';

describe('FiltroDetalleProductosPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroDetalleProductosPipe();
    expect(pipe).toBeTruthy();
  });
});
