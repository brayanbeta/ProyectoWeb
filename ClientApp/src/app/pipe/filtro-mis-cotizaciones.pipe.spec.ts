import { FiltroMisCotizacionesPipe } from './filtro-mis-cotizaciones.pipe';

describe('FiltroMisCotizacionesPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroMisCotizacionesPipe();
    expect(pipe).toBeTruthy();
  });
});
